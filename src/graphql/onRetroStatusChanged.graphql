subscription onStatusUpdated($slug: String!, $password: String) {
  retroUpdated(slug: $slug, password: $password) {
    status
    works {
      id
      votes
    }
    improve {
      id
      votes
    }
    others {
      id
      votes
    }
  }
}
