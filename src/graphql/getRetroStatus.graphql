query getRetroStatus($slug: String!, $password: String) {
  retro(slug: $slug, password: $password) {
    status
    works {
      id
      votes
    }
    improve {
      id
      votes
    }
    others {
      id
      votes
    }
  }
}
